<% if signed_in? %>
	<div class="row">
		<aside class="span4">
			
			<section>
				<%= render 'shared/micropost_form' %>
			</section>
		</aside>
	</div>
<% else %>
<%= image_tag("emptylogo.png", alt: "logo") %>
<div class="center hero-unit">
	<h1>From empty suit...</br>
		to full pursuit</h1>
</div>
	<div class="span8">
	      
	      <%= render 'shared/feed' %>
	    </div>
	</div>
<% end %>

